# BOM ↔ PDF 核对工具：完整开发教程

> 本教程覆盖 `D:\Evan\Bom-AI\` 目录下从 V1 单文件脚本、V2 改进版到 V3 模块化架构的完整演进过程。
> 无论你是完全没写过 Python 的初学者，还是想读懂/维护/扩展这套代码的工程师，都能找到对应的内容。
> 建议边读边打开对应源码文件对照学习。

---

## 目录

- [1. 这个工具是干什么的](#1-这个工具是干什么的)
- [2. 版本演进：V1 → V2 → V3](#2-版本演进v1--v2--v3)
- [3. 快速上手（最快跑起来）](#3-快速上手最快跑起来)
- [4. 环境准备](#4-环境准备)
- [5. 项目结构总览](#5-项目结构总览)
- [6. 核心原理：PDF 字体逆向解码（最有价值的部分）](#6-核心原理pdf-字体逆向解码最有价值的部分)
- [7. 数据模型 models.py](#7-数据模型-modelspy)
- [8. 配置 config.py](#8-配置-configpy)
- [9. 归一化 normalizer.py](#9-归一化-normalizerpy)
- [10. 读取器 readers](#10-读取器-readers)
- [11. 提取器 extractors](#11-提取器-extractors)
- [12. 比较器 comparators（6 种模式详解）](#12-比较器-comparators6-种模式详解)
- [13. 报告生成 reports](#13-报告生成-reports)
- [14. 图形界面 GUI](#14-图形界面-gui)
- [15. 命令行模式 CLI](#15-命令行模式-cli)
- [16. 打包成 exe 可执行程序](#16-打包成-exe-可执行程序)
- [17. Git / GitHub 版本管理](#17-git--github-版本管理)
- [18. 常见问题排查 FAQ](#18-常见问题排查-faq)
- [19. 后续开发方向](#19-后续开发方向)

---

## 1. 这个工具是干什么的

在电子硬件研发中，有两份关键文档经常要对得上：

| 文档 | 格式 | 内容 |
|---|---|---|
| **BOM（物料清单）** | Excel / CSV | 每个器件的位号（`R1`、`C12`、`U3`）、值（`10K`、`0.1uF`）、料号（MPN）、数量、封装、厂商等 |
| **原理图** | PDF | 电路图，上面标注了每个器件的位号、值、封装、料号等 |

人工核对几百上千个器件非常耗时且容易出错。这个工具做的事情就是：

> **把 Excel BOM 里的器件，和 PDF 原理图里实际画出来的器件，自动逐个核对，找出"一致 / 不一致 / 缺失 / 多余"的项，并生成报告。**

支持 6 种对比模式（详见第 12 章）：

1. **PDF → Excel 位号核对**：PDF 上的每个位号，是否在 Excel BOM 里能找到
2. **PDF → Excel MPN 核对**：Excel 里的每个料号，是否出现在 PDF 原理图上
3. **Excel ↔ Excel 位号对比**：两份 BOM 的位号集合是否一致
4. **Excel ↔ Excel MPN 对比**：两份 BOM 按料号对比数量/位号/厂商/描述
5. **PDF ↔ PDF 位号对比**：两份原理图的位号集合是否一致
6. **Excel ↔ Excel 分组对比**：两份 BOM 按"值+料号"分组对比位号

---

## 2. 版本演进：V1 → V2 → V3

这个项目不是一次性写成的，它经历了三个阶段的演进。理解这个演进过程，能帮助你更好地理解当前代码为什么这样组织。

### 2.1 V1：单文件脚本（`bom_pdf_verify.py`，约 3439 行）

最初为了快速解决问题，所有代码都写在一个文件里。**优点**是开发快、功能直接可用；**缺点**是：

- 代码长达 3439 行，维护困难
- 4 套对比逻辑（Excel-PDF、Excel-Excel、PDF-PDF、分组）各自返回**不同结构**的数据
- 报告/GUI 里到处是 `if mode == ...` 分支，越加功能越失控
- 存在明确 Bug，例如依赖检查里的 `if REQUIRED or True:`（`or True` 让检查永远失效）

### 2.2 V2：改进版（`Chat-GPT-bom_pdf_verify_v2.py`，约 3521 行）

针对 V1 的代码审查（见 `bom-change-suggestion.docx`）修复了一批实际问题：

| 修复点 | 说明 |
|---|---|
| 依赖检查 Bug | 去掉 `or True`，让缺失依赖能被正确报出来 |
| CSV 分隔符误判 | 用 `csv.Sniffer` 自动识别分隔符 |
| 位号范围展开 | `R1-R5` 在 BOM 端也能展开成 `R1 R2 R3 R4 R5` |
| 位号解析统一 | BOM 端和 PDF 端复用同一个 `_split_designator_word()` |
| 重复 MPN 聚合 | 同一料号多行先聚合（数量求和、位号合并）再比较 |
| 数量归一化 | `1`、`1.0`、`1,000` 统一后再比，避免误判 |
| 空值处理 | `10` vs 空 现在正确报告为"字段不一致" |
| 厂商/料号/描述比较 | 增加了更多字段的差异检测 |

### 2.3 V3：模块化架构（`bom_verify_v3/` 目录，当前最新）

V3 是当前主推的版本，解决了 V1/V2 最核心的**架构问题**：所有对比模式统一为一种数据结构 `CompareResult`，报告/GUI 层不再需要模式分支。项目结构从"一个大文件"拆成了清晰的包：

```
bom_verify_v3/
├─ __init__.py            # 版本号
├─ main.py                # 入口：CLI / GUI
├─ config.py              # 常量、正则、列名定义
├─ models.py              # 统一数据模型（核心！）
├─ normalizer.py          # 数值/数量/文本归一化
├─ readers/               # 文件读取
│   ├─ bom_reader.py      # 读 Excel/CSV/ODS/HTML 等
│   └─ pdf_reader.py      # 读 PDF（字体逆向解码）
├─ extractors/            # 特征提取
│   ├─ designator.py      # 位号提取
│   └─ mpn.py             # 料号提取
├─ comparators/           # 对比引擎（6 种模式）
│   ├─ base.py            # 抽象基类 + 注册表
│   ├─ excel_pdf.py
│   ├─ excel_pdf_mpn.py
│   ├─ excel_excel.py
│   ├─ excel_excel_mpn.py
│   ├─ pdf_pdf.py
│   └─ group.py
├─ reports/               # 报告生成
│   └─ generator.py
└─ gui/                   # 图形界面
    ├─ app.py
    └─ theme.py
```

本教程后续章节以 V3 为主线讲解，同时在需要时会对照 V1/V2 说明差异。

---

## 3. 快速上手（最快跑起来）

### 3.1 直接双击 exe（最省事，无需装 Python）

如果你只是想**用**这个工具，项目里已经打包好了 exe：

- `dist\Bom_Check-V1.exe` —— 图形界面版
- `dist\BomVerify_CLI.exe` —— 命令行版

把 exe 和你的 BOM、PDF 文件放在同一个文件夹，双击 GUI 版即可开始核对。

### 3.2 用 Python 源码运行

如果已经装好 Python（见下一章），运行 V3：

```bat
REM 启动图形界面
python -m bom_verify_v3.main

REM 命令行模式（注意要先去掉 --cli 再传其它参数）
python -m bom_verify_v3.main --cli --mode pdf2excel -a 你的BOM.xlsx -b 你的原理图.pdf -o 输出目录
```

---

## 4. 环境准备

### 4.1 安装 Python

1. 打开 https://www.python.org/downloads/ 下载 Windows 安装包（本项目基于 Python 3.12）
2. 安装时**务必勾选 `Add Python to PATH`**
3. 验证：

```bat
python --version
```

看到 `Python 3.12.x` 即成功。

### 4.2 安装依赖库

```bat
pip install pikepdf pdfplumber openpyxl pyinstaller
```

V3 支持更多 BOM 格式，还需要（可选，按需安装）：

```bat
pip install xlrd pyxlsb odfpy defusedxml pymupdf
```

| 库名 | 用途 | 是否必需 |
|---|---|---|
| `openpyxl` | 读写 `.xlsx` Excel | 必需 |
| `pikepdf` | 底层解析 PDF 内容流与字体（取字核心） | 必需 |
| `pdfplumber` | 拿到 PDF 每个字符的精确坐标 | 必需 |
| `pyinstaller` | 打包成 `.exe` | 仅打包时需要 |
| `xlrd` | 读旧版 `.xls` | 可选 |
| `pyxlsb` | 读二进制 `.xlsb` | 可选 |
| `odfpy` | 读 LibreOffice `.ods` | 可选 |
| `pymupdf` | 生成 Arena 风格 PDF 报告 | 可选 |

> 如果运行时报 `ModuleNotFoundError: No module named 'xxx'`，就是对应库没装。

---

## 5. 项目结构总览

### 5.1 根目录文件清单

| 文件/目录 | 说明 |
|---|---|
| `bom_pdf_verify.py` | V1 单文件源码（约 3439 行） |
| `Chat-GPT-bom_pdf_verify_v2.py` | V2 改进版源码（约 3521 行） |
| `bom_verify_v3/` | V3 模块化架构（**当前主推**） |
| `零基础开发教程.md` | 面向零基础初学者的入门教程 |
| `完整开发教程.md` | 本教程 |
| `bom-change-suggestion.docx` | 对 V1 的代码审查与架构建议 |
| `*.spec` | PyInstaller 打包配置文件 |
| `dist\`、`08-14\back_up\` | 打包产物与历史备份 |
| `.gitignore` | Git 忽略清单 |

### 5.2 V3 各模块职责（一张图）

```
                    ┌────────────────┐
     Excel/CSV ────▶│  bom_reader    │────┐
     (BOM)          └────────────────┘    │
                                          ▼
     PDF ──────────▶│  pdf_reader    │  models.py（统一数据模型）
     (原理图)        │  (字体逆向解码) │    │
                    └────────────────┘    │
                                          │
     extractors（位号/料号提取）◀──────────┘
                    │
                    ▼
     normalizer（数值/数量/文本归一化）
                    │
                    ▼
     comparators（6 种对比模式）→ 统一产出 CompareResult
                    │
                    ▼
     reports（xlsx / txt / html / pdf）  +  gui（图形界面）
```

**关键设计思想**（V3 最重要的变化）：

> 无论哪种对比模式，最终都产出 `list[CompareResult]` + `CompareStats`。
> 报告层和 GUI 层消费这个统一结构，**不再需要** `if mode == ...` 分支。

---

## 6. 核心原理：PDF 字体逆向解码（最有价值的部分）

这是整个项目**技术含量最高、最值得保留**的部分。代码在 `bom_verify_v3/readers/pdf_reader.py`。

### 6.1 为什么普通的 PDF 提取文字会失败

普通 PDF 可以直接用 `pdfplumber.extract_text()` 提取文字。但这份原理图（由 Seyond / SIPR 软件导出）用了一种特殊的**自定义字体**：

- 每个字符不是一个普通文字，而是一个"小图像"（专业术语 **Type3 位图字体**）
- 字体内部没有存放"这个图像对应哪个 Unicode 字符"的映射
- 结果：任何现成工具提取出来都是乱码 `(cid:1)(cid:2)(cid:3)` 或一片问号

OCR（图像识别）也试过，小字识别准确率只有约 26%，不可靠。

### 6.2 破解思路：发现字体里的"编号暗号"

研究 PDF 内部结构后发现一个规律：字体用**字形编号**给每个字编号，例如字符 `"C"` 的编号是 `g39`，`"a"` 是 `g68`。

关键公式：**字形编号 + 29 = 字符的 ASCII 码**

```
编号 g68  →  68 + 29 = 97  →  97 = "a" ✓
编号 g39  →  39 + 29 = 68  →  68 = "D" ✓
```

这就是本项目最核心的技术发现。

### 6.3 解码流程（四步）

#### 第 1 步：解析字体 Differences → 建立"字节码 → 字形名"对照表

`_build_code2glyph(fonts)`（`pdf_reader.py:13`）：

```python
def _build_code2glyph(fonts: dict) -> dict:
    code2glyph = {}
    for fn in fonts.keys():
        f = fonts[fn]
        enc = f.get("/Encoding")
        diffs = enc.get("/Differences", None) if enc else None
        # 解析 Differences 数组：起始码 + 一串字形名
        # 结果 gmap = {字节码: "/g39", 下一码: "/g68", ...}
        code2glyph[str(fn)] = gmap
    return code2glyph
```

#### 第 2 步：解析 ToUnicode CMap（对普通字体的兜底方案）

`_build_code2unicode(fonts)`（`pdf_reader.py:54`）：

对于自带 `/ToUnicode` 映射的字体（Type0/Type1/TrueType），直接解析 CMap，把字节码映射到真正的 Unicode 字符。这是对"非 Type3 字体"的通用处理。

#### 第 3 步：遍历内容流，还原字符

`decode_page()`（`pdf_reader.py:161`）用 `pikepdf.parse_content_stream()` 逐条读取 PDF 绘图指令，遇到文字相关指令时处理：

| PDF 指令 | 含义 | 本项目处理 |
|---|---|---|
| `BT` | 文本对象开始 | 重置变换矩阵 `tm` |
| `Tf` | 设置字体和字号 | 记录当前字体 `cur_font` 和字号 `cur_size` |
| `Tm` | 设置文本矩阵 | 更新坐标 `tm` |
| `Td` / `T*` | 文本位置偏移 | 更新坐标 |
| `TL` | 设置行距 | 记录 `tl` |
| `Tj` / `'` | 显示一段文字 | 取出字节码，翻译成字符 |
| `TJ` | 显示一段文字（带间距数组） | 逐项翻译 |

真正的翻译在 `_append_char()`（`pdf_reader.py:140`）：

```python
def _append_char(out, gmap, byte, tm, cur_size):
    glyph = gmap.get(byte)                    # 字节码 → 字形名
    m = re.match(r"/g(\d+)$", glyph or "")    # 取字形编号数值
    num = int(m.group(1)) if m else None
    ch = chr(num + 29) if num is not None else "?"  # ★核心：+29 = ASCII
    out.append((ch, tm[4], tm[5], cur_size))  # 记录字符 + 坐标 + 字号
```

> Python 内置函数说明：`chr(97)` = `"a"`（数字→字符），`ord('a')` = `97`（字符→数字）。

#### 第 4 步：按坐标拼成"单词"

`decode_pdf_blocks()`（`pdf_reader.py:217`）：

- 每个字符有了坐标（x、y）和字号
- 用 `pdfplumber` 拿到精确的字符包围盒（`_get_char_boxes()`）
- 把同一行的字符按从左到右排序，字符间距大于 `字号×0.3` 就断成两个单词
- 结果：`pages_words = {页码: [(y坐标, [单词列表])]}`，每个单词带 `(文本, x0, x1, top)`

这样不仅还原出文字，还能知道**每个单词在第几页、什么位置**，为后续"附近文字匹配"提供坐标依据。

> **注意**：`_get_char_boxes()` 每页都会重新 `pdfplumber.open()` 一次。大文件（几百页）会有性能损耗，这是 V3 预留的优化点之一（可改成打开一次、遍历所有页）。

---

## 7. 数据模型 models.py

这是 V3 架构的**地基**。代码在 `bom_verify_v3/models.py`。

### 7.1 对比状态枚举 `CompareStatus`

| 枚举值 | 中文 | 含义 |
|---|---|---|
| `MATCH` | 一致 | 两文件该项完全一致 |
| `FIELD_MISMATCH` | 字段不一致 | 主键相同，但某字段（数量/厂商等）不同 |
| `PDF_ONLY` | PDF有Excel无 | PDF 上有，BOM 里没有 |
| `EXCEL_ONLY` | 仅Excel有(PDF无此料号) | Excel 有，PDF 上没有 |
| `A_ONLY` | 仅文件A | 文件 A 有，B 没有 |
| `B_ONLY` | 仅文件B | 文件 B 有，A 没有 |
| `PENDING` | 待确认 | 值/封装在 PDF 上未匹配到，需人工确认 |
| `DESIGNATOR_DIFF` | 位号集合差异 | 分组模式中位号集合不同 |

### 7.2 对比模式枚举 `CompareMode`

| 枚举值 | 值（CLI 用） | 说明 |
|---|---|---|
| `EXCEL_VS_PDF` | `pdf2excel` | Excel BOM vs PDF 原理图（位号） |
| `EXCEL_VS_PDF_MPN` | `mpn` | Excel BOM vs PDF 原理图（料号） |
| `EXCEL_VS_EXCEL` | `excel_excel` | 两份 Excel（位号） |
| `EXCEL_VS_EXCEL_MPN` | `excelmpn` | 两份 Excel（料号） |
| `PDF_VS_PDF` | `pdf_pdf` | 两份 PDF（位号） |
| `GROUP` | `group` | 两份 Excel（分组） |

### 7.3 统一结果模型 `CompareResult`

```python
@dataclass
class CompareResult:
    key: str            # 主标识（位号或 MPN）
    status: CompareStatus  # 对比状态
    mode: CompareMode      # 属于哪种模式
    source_a: dict         # 文件A数据（BOM行 / PDF标注）
    source_b: dict         # 文件B数据
    diffs: list[FieldDiff] # 字段差异列表（status==FIELD_MISMATCH 时）
    extra: dict            # 模式特有元数据（页码、附近文字等）
```

`FieldDiff` 记录单个字段差异：`field`（字段名）、`value_a`（A 值）、`value_b`（B 值）。

### 7.4 统计模型 `CompareStats`

```python
@dataclass
class CompareStats:
    mode: CompareMode
    total_keys: int     # 主键总数
    matched: int        # 一致数
    field_mismatch: int # 字段不一致数
    only_a: int         # 仅A有
    only_b: int         # 仅B有
    pending: int        # 待确认
    designator_diff: int# 位号集合差异
    extra_info: dict    # 模式特有统计
```

`stats.summary` 属性会把上述字段转成中文键的字典，供报告直接展示。

> **为什么这很重要**：V1 里每种模式返回的数据结构都不同（有的是 `valueA/footA/near`，有的是 `rec_a/rec_b/field_diffs`，有的是 `a_pages/b_pages`），导致报告层全是分支。V3 统一成 `CompareResult` 后，报告层只需处理一种结构。

---

## 8. 配置 config.py

`bom_verify_v3/config.py` 集中了所有常量、正则和列名关键字。

### 8.1 位号正则

```python
DESIGNATOR_RE = re.compile(r"^[A-Za-z]{1,4}\d{1,4}(?:[A-Za-z]\d{1,2})?$")
PREFIXES_PARTS = set("CRLUDQJRTFXSWYKT")  # 常见器件前缀：电容/电阻/电感/IC等
```

- `[A-Za-z]{1,4}`：1~4 个字母（如 `R`、`C`、`U`、`TP`）
- `\d{1,4}`：1~4 个数字（如 `1`、`57`、`1066`）
- `(?:[A-Za-z]\d{1,2})?`：可选的子序号（如 `R1A1`）
- `PREFIXES_PARTS`：限定首字母必须是常见器件前缀，避免误识别

### 8.2 列名关键字

```python
MPN_COL_KEYS = ("mpn", "manufacturer part number", "料号", "型号", ...)
MFG_COL_KEYS = ("manufacturer", "mfg", "vendor", "厂家", "供应商", ...)
QTY_COL_KEYS = ("quantity", "qty", "数量", "pcs")
REF_COL_KEYS = ("reference", "refdes", "designator", "位号", ...)
DESIGNATOR_KEYS = ("part reference", "designator", "位号", ...)
```

这些关键字用于**自动识别列**：当用户没指定用哪一列时，程序遍历表头，匹配这些关键字（支持中英文、空格、大小写不敏感）来猜出"料号列""数量列""位号列"等。

---

## 9. 归一化 normalizer.py

比较两个文件时，不能直接比原始字符串，否则 `1` 和 `1.0`、`10K` 和 `10k`、`1,000` 和 `1000` 都会被误判为"不一致"。归一化就是把各种写法统一成可比的形式。

### 9.1 主要函数

| 函数 | 作用 | 示例 |
|---|---|---|
| `normalize_key(v)` | 去空白、转大写 | `" R218 "` → `"R218"` |
| `normalize_qty(v)` | 数量转数值 | `"1,000"` → `1000`；`"1.0"` → `1` |
| `normalize_text(v)` | 去空格/横线/点/斜杠，转大写 | `"MuRata-01"` → `"MURATA01"` |
| `normalize_cap_value(s)` | 电容值统一到 pF | `"0.1uF"` → `100000.0`（pF） |
| `normalize_res_value(s)` | 电阻值统一到 Ω | `"4R7"` → `4.7`；`"2K4"` → `2400.0` |
| `normalize_value(v)` | 智能归一化（电容/电阻/纯数字） | 自动识别类型 |
| `footprint_family(d)` | 封装归类到系列 | `"CAPC1005"` → `"0402"` |

### 9.2 电容值归一化示例

```python
def normalize_cap_value(s):
    # 单位换算表：pf→1, nf→1000, uf→1_000_000, f→1_000_000_000
    # "0.1uF" → 0.1 × 1_000_000 = 100000.0 (pF)
```

### 9.3 电阻值归一化示例（支持特殊写法）

- `4R7` → 4.7 Ω（R 当小数点）
- `2K4` → 2400 Ω
- `100R` → 100 Ω
- `1K5` → 1500 Ω

### 9.4 为什么 V3 拆成了多个函数

V1 用一个 `smart_norm_value()` 函数"大包大揽"，通过 `if "uf" in s` 这类**字符串包含**判断类型，很容易误判（比如料号 `MPN100K` 里含 `K` 会被当成电阻）。V3 拆成 `normalize_capacitor / normalize_resistor / normalize_quantity / normalize_mpn / ...` 每个字段一个专用归一化函数，可靠性更高。

---

## 10. 读取器 readers

### 10.1 BOM 读取器 `readers/bom_reader.py`

支持格式：`.xlsx`、`.xlsm`、`.xls`、`.xlsb`、`.ods`、`.html`、`.csv`、`.tsv`、`.txt`。

核心函数：

| 函数 | 作用 |
|---|---|
| `_find_header_row(rows)` | 自动定位表头行（在表可能有标题/空行的前 24 行里找含关键字最多的行） |
| `_detect_designator_col(headers)` | 自动识别位号列 |
| `_find_col(headers, keys)` | 按关键字找列 |
| `_csv_content_rows(path)` | 读 CSV/TSV，自动检测编码（utf-8/gbk/utf-16 等）和分隔符 |
| `_load_xls/_load_xlsb/_load_ods/_load_html` | 各格式专用读取 |
| `load_bom(path)` | 统一入口，返回 `(headers, data)` |

返回格式：

```python
headers = ["Item Number", "Part Reference", "Value", ...]  # 表头列表
data = [{"header_row": 3, "values": ["001", "R1,R2", "10K", ...]}, ...]
```

> 每个数据行记录 `header_row`（在 Excel 中的真实行号），方便报告里回指"第几行有问题"。

### 10.2 PDF 读取器 `readers/pdf_reader.py`

已在第 6 章详解，核心入口是 `decode_pdf_blocks(pdf_path)`，返回：

```python
pages_words  # {页码: [(y坐标, [ (单词, x0, x1, top), ... ]), ...]}
pages_text   # {页码: [(y坐标, 该行全部文字), ...]}
page_count   # 总页数
```

---

## 11. 提取器 extractors

### 11.1 位号提取 `extractors/designator.py`

位号形式多样，需要健壮的解析：

| 输入 | 期望输出 |
|---|---|
| `C2,C7` | `C2`, `C7` |
| `C300-C302` | `C300`, `C301`, `C302` |
| `R1-R5` | `R1`...`R5` |
| `R2180R0201` | `R2180`, `R0201`（连写） |
| `R86-` 换行 `R89` | `R86`...`R89`（跨行范围） |

核心函数：

- `split_designators_text(text)`：BOM 单元格文本 → 位号列表
- `extract_designators_from_pdf(pages_words)`：PDF 解码词 → `{位号: [页码]}`
- `build_pdf_designator_annotations(pages_words)`：给每个位号找"附近文字"（用于值/封装匹配）
- `extract_designators_from_excel(path)`：Excel → 位号集合

> **V2 的重要修复**：V1 里 BOM 端和 PDF 端的位号解析逻辑是两套，导致 `R1-R5` 在 PDF 端展开、BOM 端不展开，产生假差异。V2/V3 让两端复用同一个 `_split_designator_word()`。

### 11.2 料号提取 `extractors/mpn.py`

- `_extract_pdf_mpn_index(pages_words)`：从 PDF 解码词里识别料号（MPN），返回 `{MPN: [(页码, y, x)]}`
- `_linked_designators(pages_words, pno, y, x, mpn)`：找某个料号附近的位号（用于"这个料号对应哪些位号"）

MPN 识别规则（`mpn.py:12`）：

```python
mpn_re = re.compile(r"^(?=.*[A-Za-z])(?=.*\d)[A-Za-z0-9][A-Za-z0-9.\-]{4,}$")
```

即：同时含字母和数字、长度 ≥ 5、由字母数字点横线组成。

> **已知风险**：这个规则较宽泛，`R2180`、`C1005` 这种位号也可能被误判成 MPN。这是 MPN 模式后续要优化的方向（把位号/值/MPN/网络名/封装名真正分离开）。

---

## 12. 比较器 comparators（6 种模式详解）

### 12.1 框架 `comparators/base.py`

用**注册表 + 装饰器**模式组织 6 个比较器：

```python
class BaseComparator(ABC):
    mode: CompareMode
    @abstractmethod
    def compare(self, path_a, path_b, **kwargs) -> tuple[list[CompareResult], CompareStats]:
        ...

_registry: dict[CompareMode, type[BaseComparator]] = {}

def register(mode):
    def decorator(cls):
        _registry[mode] = cls
        cls.mode = mode
        return cls
    return decorator

def run_comparison(mode, path_a, path_b, **kwargs):
    return get_comparator(mode)().compare(path_a, path_b, **kwargs)
```

每个比较器用 `@register(CompareMode.XXX)` 装饰，自动注册。新增一种对比模式只需写一个类并注册，**不改动报告/GUI 层**。

### 12.2 模式一：PDF → Excel 位号核对（`excel_pdf.py`）

类：`PdfToExcelComparator`，注册为 `EXCEL_VS_PDF`。

流程：

1. 解码 PDF → 得到所有位号及"附近文字"（`build_pdf_designator_annotations`）
2. 读 Excel BOM → 按位号建索引 `{位号: [行,...]}`
3. 遍历 PDF 的每个位号：
   - BOM 里没有 → `PDF_ONLY`（PDF 有 Excel 无）
   - BOM 里有 → 拿该行的 `Value`/`PCB Footprint`，去"附近文字"里找匹配
     - 匹配到 → `MATCH`（一致）
     - 没匹配到 → `PENDING`（待确认）

值匹配逻辑（`excel_pdf.py:64-86`）：把 Excel 值、归一化后的值、封装等作为候选，逐个和附近文字比（精确匹配、归一化匹配、短数字包含匹配）。

> **诚实说明**："附近文字匹配"是**空间启发式**算法（`abs(y)<=120` 且 `abs(x)<=260` 的文字都算"附近"），不是严格的器件级匹配。所以结果标注为"疑似一致/待确认"，而非 100% 确定。

### 12.3 模式二：PDF → Excel MPN 核对（`excel_pdf_mpn.py`）

类：`PdfToExcelMpnComparator`，注册为 `EXCEL_VS_PDF_MPN`。

流程：

1. 读 Excel，识别 MPN 列、数量列、位号列、厂商列
2. 解码 PDF → `_extract_pdf_mpn_index` 得到 PDF 上的料号索引
3. 遍历 Excel 每个料号：
   - PDF 上找不到 → `EXCEL_ONLY`
   - PDF 上有 → 用 `_linked_designators` 找该料号附近的位号，对比数量/位号
     - 数量、位号都吻合 → `MATCH`
     - 有提示（数量不符、位号缺失）→ `PENDING`

### 12.4 模式三：Excel ↔ Excel 位号对比（`excel_excel.py`）

类：`ExcelExcelComparator`，注册为 `EXCEL_VS_EXCEL`。

流程：分别从两份 Excel 提取位号集合，做集合运算：

```python
common = set_a & set_b   # 两边都有 → MATCH
only_a = set_a - set_b   # 仅A有 → A_ONLY
only_b = set_b - set_a   # 仅B有 → B_ONLY
```

### 12.5 模式四：Excel ↔ Excel MPN 对比（`excel_excel_mpn.py`）

类：`ExcelExcelMpnComparator`，注册为 `EXCEL_VS_EXCEL_MPN`。这是**最复杂**的一个模式。

**核心亮点：重复料号聚合**（V2 引入的关键修复）。

一份 BOM 里同一个 MPN 可能占多行：

```
MPN=ABC123  Qty=10  Ref=R1,R2
MPN=ABC123  Qty=20  Ref=R3,R4
```

正确做法是先聚合，而不是只取第一行比较：

```python
def aggregate(records):
    # 数量求和、位号合并去重、厂商/料号/描述取第一个
    return {"qty": 30, "refs": ["R1","R2","R3","R4"], ...}
```

聚合后对比字段：**数量、位号、厂商、内部料号、描述**，任一不同即产生 `FieldDiff`，归入 `FIELD_MISMATCH`。

> **空值处理修复**：V1 有 `if qa_ != qb_ and qa_ and qb_:`，导致 `10` vs 空 不报差异。V2/V3 去掉 `and qa_ and qb_`，空值也能正确报差异。

### 12.6 模式五：PDF ↔ PDF 位号对比（`pdf_pdf.py`）

类：`PdfPdfComparator`，注册为 `PDF_VS_PDF`。与模式三逻辑相同，只是数据源换成两份 PDF 的解码位号。

### 12.7 模式六：Excel ↔ Excel 分组对比（`group.py`）

类：`GroupComparator`，注册为 `GROUP`。

按"分组键"（默认 `Value` + `Manufacturer PN`）把器件分组，对比每组下的位号集合是否一致：

- 只在一份里有 → `A_ONLY` / `B_ONLY`
- 两边都有但位号集合不同 → `DESIGNATOR_DIFF`
- 完全相同 → `MATCH`

适用于"两个版本的 BOM 用了哪些料、各料挂了哪些位号"的整体对比。

---

## 13. 报告生成 reports

代码在 `bom_verify_v3/reports/generator.py`。

### 13.1 每种模式定义自己的列布局

`MODE_COLUMNS` 字典为每种 `CompareMode` 定义表头和取值路径：

```python
MODE_COLUMNS = {
    CompareMode.EXCEL_VS_PDF: {
        "sheet_title": "PDF→Excel 器件核对",
        "columns": [
            ("PDF位号", "key"),
            ("核对结果", "status"),
            ("Excel值(Value)", "extra.valueA"),
            ("Excel封装(Footprint)", "extra.footA"),
            ("Excel数量", "extra.qty"),
            ("PDF附近标注", "extra.near"),
        ],
    },
    ...
}
```

取值路径用点号分隔（如 `extra.valueA`、`source_a.qty`），由 `_resolve_value()` 解析。这样报告层只需"按列布局取值"，完全不知道模式细节。

### 13.2 生成的文件

`generate_report()` 依次生成：

| 文件 | 说明 |
|---|---|
| `对比报告_时间戳.xlsx` | Excel 报告：明细表（不一致行标红）+ 汇总表 |
| `对比报告_时间戳.txt` | 纯文本报告 |
| `对比报告_时间戳.html` | 网页报告（表格样式，一致绿/不一致红） |
| `对比报告_时间戳.pdf` | Arena 风格 PDF 报告（**仅 Excel-Excel MPN 模式**） |

Arena 风格 PDF 报告（`_generate_pdf()`）用 `pymupdf` 绘制，模仿 Seyond 生产系统的 BOM 对比报告格式：页眉公司名、文件标题、`>>` 标记变更行、`ITEM NUMBER / ITEM NAME / PCS / REF DES / SUBSTITUTES` 列。

> **V1 的 Bug**：原来 `if mode not in ("excelmpn", "mpn")` 会为 Excel-PDF 模式错误生成 Arena 风格 PDF，但数据结构不匹配。V3 修正为只有 `EXCEL_VS_EXCEL_MPN` 才生成。

---

## 14. 图形界面 GUI

代码在 `bom_verify_v3/gui/app.py`（约 413 行）+ `theme.py`。

### 14.1 界面布局

```
┌─────────────────────────────────────────────┐
│ BOM ↔ PDF 原理图 器件核对工具 V3   [导出报告] │
├─────────────────────────────────────────────┤
│ 对比设置： 对比类型 ▾   核对方式 ▾            │
├─────────────────────────────────────────────┤
│ 文件选择： 文件A ▾  文件B ▾  [开始核对]       │
│            [=========进度条==========]        │
├─────────────────────────────────────────────┤
│ 对比结果： ┌──────────────────────────────┐  │
│            │  表格（一致绿 / 不一致红）    │  │
│            └──────────────────────────────┘  │
├─────────────────────────────────────────────┤
│ 状态栏                                       │
└─────────────────────────────────────────────┘
```

### 14.2 三种对比类型

```python
COMPARE_TYPES = ["Excel vs Excel", "Excel vs PDF", "PDF vs PDF"]
SUB_MODES_EXCEL_PDF  = ["位号核对(推荐)", "全文匹配", "位号对比"]
SUB_MODES_EXCEL_EXCEL = ["MPN对比"]
SUB_MODES_PDF_PDF     = ["位号对比"]
```

切换"对比类型"时，`_on_compare_change()` 会更新"核对方式"下拉框的可选项。

### 14.3 关键技术点

| 技术点 | 作用 |
|---|---|
| DPI 适配 `_enable_dpi()` / `_detect_scaling()` | 高分屏下界面不模糊 |
| 配置记忆 `_load_config()/_save_config()` | 记住上次选的文件和模式（存到 `~/.bom_verify_v3_config.json`） |
| 后台线程 `Thread(target=work, daemon=True)` | 解码 PDF 等耗时操作不卡界面 |
| `root.after(0, _on_complete)` | 后台完成后回主线程刷新表格 |
| `ttk.Treeview` + `tag_configure("ok"/"bad")` | 结果表格，一致绿、不一致红 |
| `filedialog` / `askdirectory` | 选文件、选输出目录 |

### 14.4 配色主题 `theme.py`

```python
class Theme:
    BG = "#F4F6FB"      # 页面背景
    BRAND = "#2C6FBB"   # 主色（蓝）
    OK_BG = "#E7F7EA"   # 一致=绿底
    OK_FG = "#1E7B34"
    BAD_BG = "#FDEAEA"  # 不一致=红底
    BAD_FG = "#C0392B"
    ...
```

改界面颜色只需改这一个文件。

---

## 15. 命令行模式 CLI

代码在 `bom_verify_v3/main.py`。

### 15.1 用法

```bat
python -m bom_verify_v3.main --cli --mode <模式> -a <文件A> -b <文件B> -o <输出目录>
```

### 15.2 模式映射（`MODE_MAP`）

| CLI 参数值 | 对应模式 |
|---|---|
| `pdf2excel` | Excel vs PDF（位号） |
| `mpn` | Excel vs PDF（料号） |
| `excel_excel` | Excel vs Excel（位号） |
| `excelmpn` | Excel vs Excel（料号） |
| `pdf_pdf` | PDF vs PDF（位号） |
| `group` | Excel vs Excel（分组） |

### 15.3 示例

```bat
REM Excel BOM 和 PDF 原理图做料号核对
python -m bom_verify_v3.main --cli --mode mpn -a 870-000837_REV_A.BOM.xlsx -b 870-000837_rev_a.pdf -o 报告

REM 两份 Excel BOM 做 MPN 对比
python -m bom_verify_v3.main --cli --mode excelmpn -a BOM_A.xlsx -b BOM_B.xlsx -o 报告
```

CLI 和 GUI 共用同一套核心（`run_comparison` + `generate_report`），只是不画界面、直接打印结果到控制台。

---

## 16. 打包成 exe 可执行程序

### 16.1 原理

`pyinstaller` 把你的 `.py` + 依赖库 + Python 解释器打成一个绿色单文件 `.exe`，别人电脑不用装 Python 也能运行。

### 16.2 打包命令

```bat
REM V3 图形界面版
python -m PyInstaller --onefile --windowed --name BomVerify_GUI --collect-all pikepdf bom_pdf_verify.py

REM V3 命令行版（保留控制台窗口）
python -m PyInstaller --onefile --console --name BomVerify_CLI bom_pdf_verify.py
```

参数解释：

| 参数 | 意思 |
|---|---|
| `--name 名字` | exe 文件名 |
| `--onefile` | 打成单个 exe（方便拷贝） |
| `--windowed` | 不显示黑色控制台（GUI 版） |
| `--console` | 保留控制台（CLI 版） |
| `--collect-all pikepdf` | pikepdf 有动态加载的资源文件，必须全部收集，否则运行报错 |

### 16.3 spec 文件

项目里已有多份 `.spec` 文件（`Bom_Check-V1.spec` 等），它们就是"打包配置"，保存了分析结果和参数。V1 的 spec 里用 `collect_all()` 收集了所有依赖（pikepdf、pdfplumber、pyxlsb、odfpy、defusedxml、docx、pymupdf），并设置 `upx=True`（压缩）。

用 spec 打包：`python -m PyInstaller Bom_Check-V1.spec`

### 16.4 关键：收集 pikepdf

`pikepdf` 依赖原生库和字体等资源文件，打包时必须：

```python
from PyInstaller.utils.hooks import collect_all
tmp_ret = collect_all('pikepdf')
datas += tmp_ret[0]; binaries += tmp_ret[1]; hiddenimports += tmp_ret[2]
```

否则 exe 运行时可能报"找不到 QPDF 库"之类的错误。

---

## 17. Git / GitHub 版本管理

项目已纳入 Git 管理（远程：https://github.com/dreamsdrny/Bom-Check-PDF）。

### 17.1 `.gitignore`（忽略清单）

```gitignore
__pycache__/     # Python 字节码缓存
*.py[cod]        # 编译缓存
dist/            # 打包产物
build/           # 打包临时目录
08-14/back_up/   # 备份目录
*.exe            # 可执行文件
~$*              # Office 临时文件
*.mp4            # 视频文件
```

> 规则：末尾带 `/` 表示忽略**整个文件夹**；不带 `/` 的通配符匹配**文件**。

### 17.2 常用工作流

```bash
git status                      # 看当前改动
git remote -v                   # 看远程仓库
git branch -M main              # 重命名分支为 main
git add .                       # 加入暂存区
git commit -m "说明"             # 本地打包成一个版本
git push -u origin main         # 推送并记住默认位置
```

删除已提交的文件：

```bash
git rm "某.xlsx" "某.pdf"
git commit -m "remove sample BOM and PDF files"
git push
```

> 建议：**不要**把样例 BOM/PDF 提交到公开仓库（可能含公司敏感信息），本仓库已删除样例数据。

---

## 18. 常见问题排查 FAQ

| 问题 | 可能原因 | 解决办法 |
|---|---|---|
| `ModuleNotFoundError: No module named 'xxx'` | 依赖没装 | `pip install xxx` |
| PDF 提取全是乱码/问号 | 原理图用了 Type3 自定义字体 | 本项目已用字体逆向解码处理；如仍失败，确认 PDF 是同类导出格式 |
| 位号明明在 PDF 上却报"不一致" | 位号解析/大小写/范围展开问题 | 检查位号写法是否符合正则（`R1`、`C100` 等） |
| 数量 `1` 和 `1.0` 被判不一致 | 未归一化 | V3 已用 `normalize_qty` 处理 |
| exe 运行报错找不到库 | pikepdf 资源未打包 | 打包时加 `--collect-all pikepdf` |
| GUI 卡死无响应 | 耗时操作在主线程 | V3 已放入后台线程 |
| 高分屏界面模糊 | 未启用 DPI 感知 | 已用 `SetProcessDpiAwareness` 处理 |

---

## 19. 后续开发方向

根据 `bom-change-suggestion.docx` 的架构建议，这个工具下一步的演进方向：

1. **统一数据流**（V3 已完成）：`CompareResult` 统一结构。
2. **重构 PDF 解码性能**：`pdfplumber.open()` 改为打开一次、遍历所有页，支持几百页大文件。
3. **强化 MPN 识别**：把位号/值/MPN/网络名/封装名真正分离，减少误判。
4. **封装等级匹配**：`Footprint` 拆成"精确匹配 / 系列匹配 / 未知"三级。
5. **扩展到 PCB 领域**：BOM ↔ Schematic ↔ PCB ↔ ODB++ ↔ Gerber，最终形成 **PCB Engineering Verification Platform**（BOM Check / Schematic Check / PCB Check / DFM Check 等）。

新增一种对比（比如 BOM vs PCB）时，V3 架构下只需**新增一个 reader + 一个 comparator**，无需重写比较逻辑和报告层。

---

## 附录 A：V3 完整代码结构速查表

```
bom_verify_v3/
├─ __init__.py            版本号
├─ main.py                入口（main / main_cli / MODE_MAP）
├─ config.py              正则、列名关键字、编码表
├─ models.py              CompareMode / CompareStatus / CompareResult / CompareStats / FieldDiff
├─ normalizer.py          归一化（数量/电容/电阻/文本/主键/封装）
├─ readers/
│   ├─ bom_reader.py      多格式 BOM 读取 + 表头自动识别
│   └─ pdf_reader.py      PDF 字体逆向解码（核心）
├─ extractors/
│   ├─ designator.py      位号提取（范围展开、连写、跨行）
│   └─ mpn.py             料号提取 + 附近位号关联
├─ comparators/
│   ├─ base.py            抽象基类 + 注册表
│   ├─ excel_pdf.py       模式1：PDF→Excel 位号
│   ├─ excel_pdf_mpn.py   模式2：PDF→Excel 料号
│   ├─ excel_excel.py     模式3：Excel↔Excel 位号
│   ├─ excel_excel_mpn.py 模式4：Excel↔Excel 料号（含聚合）
│   ├─ pdf_pdf.py         模式5：PDF↔PDF 位号
│   └─ group.py           模式6：分组对比
├─ reports/
│   └─ generator.py       报告（xlsx/txt/html/pdf）+ 列布局
└─ gui/
    ├─ app.py             主界面（DPI/配置/线程/表格）
    └─ theme.py           配色主题
```

## 附录 B：Python 关键字速记（V1 教程补充）

| 关键字 | 作用 |
|---|---|
| `global` | 修改**全局作用域**（最外层）变量 |
| `nonlocal` | 修改**外层嵌套函数（闭包外层）**的变量 |
| 不加关键字 | 只读外层变量；一旦赋值就新建一个**局部变量** |

---

祝学习顺利！遇到问题按顺序排查：环境 → 数据格式 → 核心解码 → 对比逻辑 → 报告 → 打包。
