# BOM ↔ PDF 核对小工具：零基础开发教程

> 面向对象：完全没写过 Python 的初学者
> 本文以项目里的 `bom_pdf_verify.py` 为例，一步步讲清：怎么搭环境、功能怎么做出来的、界面为什么那样设计、最后如何打包成 exe。
> 配套文件在 `D:\Evan\Bom-AI\` 目录下，边读边打开代码对照，效果最好。

---

## 目录

1. [准备工作：装 Python 和工具库](#第1步准备工作装-python-和工具库)
2. [先想清楚：我们要做一个什么程序](#第2步先想清楚做一个什么程序)
3. [第1块拼图：读取 Excel BOM](#第3块拼图读取-excel-bom)
4. [第2块拼图：把 PDF 里的字"读"出来（难点）](#第4块拼图把-pdf-里的字读出来难点)
5. [第3块拼图：逐一核对并统计结果](#第5块拼图逐一核对并统计结果)
6. [第4块拼图：生成报告文件](#第6块拼图生成报告文件)
7. [第5块拼图：图形界面 GUI](#第7块拼图图形界面-gui)
8. [可选拼图：二级窗口（详情/汇总）](#第8块拼图二级窗口详情汇总)
9. [命令行模式：不用界面也能跑](#第9块拼图命令行模式)
10. [打包成 exe 可执行程序](#第10块拼图打包成-exe)
11. [对照练习：改一改，练一练](#第11块拼图对照练习)

---

## 第1步：准备工作：装 Python 和工具库

### 1.1 安装 Python

本项目用到的源码是 `.py` 文件，电脑要先装 Python 才能运行。

1. 打开官网 https://www.python.org/downloads/ 下载 Windows 安装包
2. 双击安装时，**务必勾选 `Add Python to PATH`**（这样在命令行直接敲 `python` 就能用）
3. 装完验证：打开"命令提示符"(Win+R 输入 `cmd`)，输入：

```bat
python --version
```

看到类似 `Python 3.12.x` 就代表成功。

### 1.2 安装工具库（叫"第三方依赖"）

平时 Python 只自带基础功能，读写 Excel、解析 PDF 需要别人造好的轮子（第三方库）。用 `pip` 一键安装：

```bat
pip install pikepdf pdfplumber openpyxl pyinstaller
```

| 库名 | 用途 |
|---|---|
| `openpyxl` | 读写 `.xlsx` Excel 文件 |
| `pikepdf` | 底层解析 PDF 内部结构（本项目取字的核心） |
| `pdfplumber` | 拿到 PDF 每个字符的精确坐标 |
| `pyinstaller` | 把 `.py` 打包成 `.exe` |  

> 如果装完后运行程序提示 `ModuleNotFoundError`，就是这一步没装齐。

**小结**：环境 = Python + 4 个库。之后所有开发都在同一个目录 `D:\Evan\Bom-AI\` 里进行。

---

## 第2步：先想清楚，做一个什么程序

### 2.1 用户的需求

 BOM 表（Excel，里面列了上百个器件位号）和一份原理图（PDF）。

需求是：
- **把 Excel 里的某个列（比如器件位号 C1、R2、U3...）和 PDF 原理图里画到的器件，逐个核对**
- 核对完生成一份报告
- 一致的显示"一致"，不一致的显示出来

### 2.2 拆解成 5 个小任务

写程序前先把大问题拆小（很重要！）：

```
读取 Excel 表        →  任务 A
把 PDF 里的字读出来  →  任务 B（最难）
Excel 的每个位号去 PDF 里找  →  任务 C
把结果写成报告      →  任务 D
做个图形界面让用户点  →  任务 E
```

每个"任务"在代码里就是一个"函数"。程序 = 若干函数拼起来。

### 2.3 程序的"总指挥" main()

代码最下面的 `main()`（第 1105 行）就是总指挥，它决定：进图形界面，还是跑命令行。

```python
def main():
    ...
    if args.check:              # 用户带了 --check 参数
        sys.exit(run_cli(...))  #    → 跑命令行核对
    run_gui()                   # 否则（默认）→ 打开图形界面
```

Python 程序从 `if __name__ == "__main__": main()` 这行开始执行（第 1132 行）。这是固定写法，记住即可。

---

## 第3块拼图：读取 Excel BOM

### 3.1 函数 `load_bom()`（第 235 行）

功能：打开 `.xlsx`，把表头和所有行读进来。

```python
def load_bom(bom_path, sheet_contains=None):
    wb = openpyxl.load_workbook(bom_path, data_only=True)  # 打开工作簿
    ws = wb.active                                          # 取第一张工作表
    headers = []                                            # 表头，如 ["Item Number","Part Reference",...]
    data = []                                               # 数据行
    for r in range(1, ws.max_row + 1):                      # 逐行遍历
        row = []
        for c in range(1, ws.max_column + 1):               # 逐列遍历
            v = ws.cell(row=r, column=c).value
            row.append(v if v is not None else "")          # 空单元格补成 ""
        if r == 1:
            headers = [str(x) for x in row]                 # 第1行当表头
        else:
            data.append({"header_row": r, "values": row})   # 记录行号+内容
    return headers, data
```

### 3.2 关键概念讲解

| 术语 | 含义 | 代码示例 |
|---|---|---|
| 变量 | 存数据的"盒子" | `headers = []` |
| 函数 | 写一段可重复调用的代码 | `def load_bom(...)` |
| 循环 | 重复做事 | `for r in range(1, ws.max_row+1)` |
| 字典 dict | 用"键"取"值"的盒子 | `{"header_row": r, "values": row}` |
| 元组/返回多个值 | 一次返回几样东西 | `return headers, data` |

> **给初学者的建议**：看到不认识的代码就查字典——`headers = [str(x) for x in row]` 意思是"把 row 里的每个元素 x 变成字符串，组成一个新列表"，这行叫**列表推导式**，是 Python 的便捷写法。

---

## 第4块拼图：把 PDF 里的字"读"出来（难点）

### 4.1 为什么这个最难

普通 PDF 可以直接用 `pdfplumber` 提取文字，但这个项目里的 PDF **不能**：

打开 `870-000837_rev_a.pdf`，无论用哪个库提取，出来的都是乱码：`(cid:1) (cid:2) (cid:3)...` 或一片问号。

原因：这份原理图（SEYOND/SIPR 软件导出）用了**自定义字体**——每个字不是普通文字，而是一个小"图像"（专业术语叫 Type3 位图字体），并且字体里没有存放"这个图像是哪个字"的说明书。所以任何现成工具都读不出来。

OCR（拍照识别文字）试过，但小字识别准确率只有 26%，太不可靠。

### 4.2 破解思路：发现字体里的"暗号"

我研究了 PDF 内部结构，发现了一个规律——它的字体用"字形编号"把每个字编了号，比如字 "C" 的编号是 `g39`，"a" 是 `g68`。

把编号翻译成字符，计算：**编号 + 29 = 字符的 ASCII 码**。例如：

```
编号 g68  →  68+29=97  →  97 是字母 "a" 的代码 ✓
编号 g39  →  39+29=68  →  68 是字母 "D" 的代码 ✓
```

这就是本项目最关键的技术发现，代码在 `_build_code2glyph()`（第 47 行）和 `_append_char()`（第 137 行）。

### 4.3 三步把字读出来

#### (1) 建立"编号→字母"对照表  `_build_code2glyph()`

```python
def _build_code2glyph(fonts):
    code2glyph = {}
    for fn in fonts.keys():
        ...  # 把每个字体里 Differences 记录解析出来
        gmap[字符码] = 字形名    # 例如 {1: "/g39", 2: "/g68", ...}
        code2glyph[str(fn)] = gmap
    return code2glyph
```

#### (2) 关键解码函数  `decode_page()`（第 88 行）

读 PDF 的"内容流"，碰到画字的指令（`Tj`、`TJ`）时，把里面的字节码拿去对照表翻译，再 `+29` 还原成真的字符：

```python
def _append_char(out, gmap, byte, tm, cur_size):
    glyph = gmap.get(byte)                    # 字节 → 字形编号
    m = re.match(r"/g(\d+)$", glyph or "")    # 取出编号数值
    num = int(m.group(1)) if m else None
    ch = chr(num + 29)                        # ★核心：+29 = ASCII码
    out.append((ch, tm[4], tm[5], cur_size))  # 记住字符和它的坐标
```

> 这里 `chr(数)` 是 Python 内置函数，把数字变成字符；`ord('a')` 反之。`chr(97)` = "a"。

#### (3) 按坐标拼成"单词"  `decode_pdf_blocks()`（第 145 行）

单字的坐标有了（在哪一行、哪个位置），就把同一行的字按左右顺序连起来，两个字的间距多大就当成空格——这样还原出完整的文字，还能知道每个位号在**第几页**。

### 4.4 提取位号  `extract_designators()`（第 220 行）

位号长这样：`C1`、`R57`、`U44` —— 即"字母开头 + 数字"。用正则把符合这种样式的单词捞出来：

```python
DESIGNATOR_RE = re.compile(r"^[A-Za-z]{1,4}\d{1,4}$")   # 例：R112、C1066
```

结果存成字典 `{位号: [出现在第几页, ...]}`，供核对使用。

> **建议**：正则就是"按规则在文字里找东西"。`[A-Za-z]` 表示字母，`\d` 表示数字，`{1,4}` 表示 1~4 个，`^...$` 表示开头到结尾。规则写在 `/.../` 里。
> 想练正则可以去 https://regex101.com 剪贴测试，看着高亮理解每个符号。

---

## 第5块拼图：逐一核对并统计结果

### 5.1 位号核对  `check_designators()`（第 282 行）

- 从 Excel 指定列（比如 `Part Reference`）取出每个位号；
- Excel 里一行可能写了好几个位号如 `C17,C1087,C1088,C1090`，先用 `split_designators_text()` 按逗号拆开（第 272 行附近）；
- 逐个判断：`这个位号在不在 PDF 的位号表里？` —— 用一句 `d in pdf_tokens` 搞定。

```python
for d in dlist:
    found = d in pdf_tokens                      # 判断（True/False）
    pages = pdf_tokens.get(d, []) if found else []
    dres.append({"designator": d, "found": found, "pages": pages})
```

### 5.2 全文匹配  `check_value_in_pdf()`（第 310 行）

如果用户选了别的列（比如 `Value` 电容值 `10nf`），就把它当关键词，在 PDF 还原出的所有单词里找有没有一样的：

```python
hits = [p for p, y, w, x0, x1, top in pdf_all_words if w.upper() == v.upper()]
```

`w.upper()` 全部转大写再比，避免大小写问题。

### 5.3 统计  `CompareResult.stat()`

遍历所有位号，数一数：总数 / 一致的 / 不一致的，再把不一致的存起来准备展示。这就是"核对完成后给用户看结论"的数据来源。

---

## 第6块拼图：生成报告文件

函数 `gen_report()`（第 351 行）。用 `openpyxl` 生成一个 Excel，包含 3 张表：

| 工作表 | 内容 |
|---|---|
| 核对明细 | 每个位号：BOM行 / 列 / 值 / 是否找到 / 页码 / 结果 |
| 汇总 | 总数、一致数、不一致数、PDF额外位号 |
| PDF全部位号 | PDF里所有位号及页码，标注是否在BOM |

同时也输出一份 `.txt` 文本报告。代码核心就是：

```python
ws.append(["BOM行号","列","原值","位号/值","PDF中是否存在","所在页码","结果"])
for row in rows:
    for d in row["designators"]:
        ws.append([row["header_row"], row["col_name"], row["raw"],
                   d["designator"], "是" if d["found"] else "否", "...", "一致" if d["found"] else "不一致"])
```

`ws.append(一行列表)` 就是往 Excel 加一行，非常直观。

---

## 第7块拼图：图形界面 GUI

### 7.1 用什么写 GUI

本项目用 **tkinter**——Python 自带的图形界面库，不需要额外安装。虽然长得朴素，但零依赖、够用。

界面由很多"控件"组成：按钮 Button、下拉框 Combobox、表格 Treeview、输入框 Entry、标签 Label 等。

### 7.2 配色主题  `Theme` 类（第 427 行）

用一个大类统一存颜色，界面各处引用来保证风格一致：

```python
class Theme:
    BG      = "#F4F6FB"   # 页面背景
    CARD    = "#FFFFFF"   # 卡片底色
    BRAND   = "#2C6FBB"   # 主色（蓝）
    BRAND_DK= "#1E4E84"
    OK_BG   = "#E7F7EA"   # 一致=绿
    BAD_BG  = "#FDEAEA"   # 不一致=红
    ...
```

想让按钮变绿色、表格行变粉红，改这些颜色值即可。`#RRGGBB` 是颜色代码（红R绿G蓝B），可以上网搜"颜色代码表"选色。

### 7.3 界面布局  `run_gui()`（第 689 行）

用最朴素的排版：顶部横幅 + 左侧三张"卡片"（选文件 / 核对设置 / 二级窗口按钮）+ 右侧（数据预览 + 结果表格）+ 底部状态栏。

核心控件用法（到处都在用，记住这4条就够）：

```python
ttk.Label(父窗口, text="要显示的文字")        # 一段文字
ttk.Button(父窗口, text="按钮文字", command=函数名)  # 一个按钮，点击=调用函数
ttk.Entry(父窗口, textvariable=变量)           # 一个输入框
ttk.Treeview(父窗口, columns=("列1","列2"), show="headings")  # 一张表格
```

**占位逻辑**：先把空界面搭好，真正跑核对在后台线程里做，完成后回到主线程把表格填满（`_fill_results()`，第 987 行附近），就变色：

```python
tree.tag_configure("ok",  background=Theme.OK_BG)   # 一致的表格行 → 绿底
tree.tag_configure("bad", background=Theme.BAD_BG)  # 不一致的 → 红底
```

### 7.4 为什么把核对放到"线程"里

`_do_check()`（第 908 行附近）里有一句：

```python
threading.Thread(target=_do_check, ...).start()
```

原因是：界面（tkinter）平时一直忙着自己的事——等待鼠标点击。如果你在主线程里做耗时工作（解码 PDF），界面就会"卡死"没反应。所以让费时的活去"后台线程"做，做完再用 `root.after(0, 刷新函数)` 把结果显示回界面。这是图形程序的常见套路。

---

## 第8块拼图：二级窗口（详情/汇总）

一共做了 4 个"弹出窗口"（Toplevel）：

| 函数 | 位置 | 功能 |
|---|---|---|
| `open_pdf_text_win()` | 503 行 | 左边选页码，右边看该页还原出的全部文字 |
| `open_detail_win()`   | 570 行 | 双击结果行，看某个位号的详细上下文 |
| `open_mismatch_win()` | 621 行 | 集中列出不一致项 + PDF额外器件 |
| `open_summary_win()`  | 667 行 | 只显示统计数字 |

它们都是 `tk.Toplevel(root)` = "从主窗口弹出新窗口"，结构一样：

```python
def open_summary_win(root, app):
    win = tk.Toplevel(root)      # 1. 建新窗口
    win.title("核对汇总")         # 2. 给窗口起标题
    ...                           # 3. 往窗口里放控件
```

理解为"开一张新的空白纸，往上贴控件"即可。

---

## 第9块拼图：命令行模式

函数 `run_cli()`（第 1054 行）。如果不想开界面、想自动化（比如天天跑一次），用命令行：

```bat
BomVerify_CLI.exe --check --bom 870-000837_REV_A.BOM.xlsx --pdf 870-000837_rev_a.pdf
```

它与 GUI 走的是**同一套**核心函数（`decode_pdf_blocks`、`check_designators`、`gen_report`），只是不画界面、直接在黑屏窗口打印结果。这就是"接口与实现分离"的好处——界面和逻辑分开写，一份逻辑两处用。

---

## 第10块拼图：打包成 exe

### 10.1 原理一句话

`pyinstaller` 把你的 `.py` + 用到的所有库代码 + Python 解释器，打成一个绿色单文件 `.exe`。别人电脑不用装 Python 也能双击运行。

### 10.2 打包命令

在项目目录 `D:\Evan\Bom-AI` 打开命令行，执行：

```bat
REM 图形界面版（双击打开窗口，不显示黑色控制台）
python -m PyInstaller --name BomVerify_GUI --windowed --onefile bom_pdf_verify.py

REM 命令行版（黑窗口里打印结果）
python -m PyInstaller --name BomVerify_CLI --console --onefile bom_pdf_verify.py
```

参数解释：

| 参数 | 意思 |
|---|---|
| `--name 名字` | exe 取什么名字 |
| `--onefile` | 打包成单个 exe 文件（方便拷贝） |
| `--windowed` | 不让黑乎乎的控制台窗口出现 |
| `--console` | 保留控制台窗口（命令行版需要） |

打包成功后，exe 在 `dist\` 文件夹里。

### 10.3 测试 exe

- 双击 `BomVerify_GUI.exe` → 应弹出图形界面
- 命令行里敲 `BomVerify_CLI.exe --check --bom xxx.xlsx --pdf yyy.pdf` → 应打印结果并生成报告

> 小提示：exe 请在**项目目录**里运行（程序会自动查找同目录的 BOM 和 PDF）。把 exe 和两个数据文件放同一个文件夹拷给别人最省事。

---

## 第11块拼图：对照练习

试着改一改就能加深理解：

| 想改什么 | 改哪里 |
|---|---|
| 窗口标题 | `run_gui()` 里 `root.title("...")` |
| 界面颜色 | `Theme` 类里的色值 |
| 一致/不一致的颜色 | `_fill_results()` 里的 `tag_configure("ok"/"bad", ...)` |
| 默认核对哪一列 | `load_bom` 后 `lb_col.set("Part Reference")`，改成别的列名 |
| 报告输出位置 | `gen_report()` 里的 `out_dir` |
| 报告里加一列 | `gen_report()` 的 `ws.append([...])` 加内容 |
| PDF 解码正负号规则 | `_append_char()` 里的 `+29` |

每次改完，先跑 `python bom_pdf_verify.py` 看效果，满意了再按第10步重新打包。

---

## 附：完整代码结构速查表

```
bom_pdf_verify.py
│
├─ 读取Excel        load_bom()              → 列名 + 行数据
├─ 解析PDF(核心)    decode_pdf_blocks()      → 每页每行文字+坐标
│                    └ _build_code2glyph()   → 字体"暗号表"
│                    └ decode_page()         → +29 还原字符
│                    └ extract_designators() → 找出 {位号:页码}
├─ 核对逻辑         check_designators()      → 位号逐个比
│                    check_value_in_pdf()    → 关键词全文找
├─ 报告             gen_report()             → xlsx + txt
├─ 图形界面         run_gui()                → 主界面
│                    open_*_win()            → 4个二级窗口
├─ 命令行           run_cli()                → 不开界面也能跑
└─ 入口             main()                   → 决定进GUI还是CLI
```

祝学习顺利！任何一步卡住，都可以回来对照本文一步步检查：环境 → 数据 → 算法 → 界面 → 打包。


Git 与Github 进行配置在VScode中
安装和创建仓库
第1步：看仓库现状
git status 和 git remote -v
第2步：创建 .gitignore（忽略文件清单）
新建了 .gitignore 文件，
内容为：  （斜杠 `/` 在末尾 = **代表这是一个文件夹** 没有末尾 `/` → 代表**文件通配符，匹配文件** ）
__pycache__/  ``Python 运行自动生成的字节码缓存文件夹，不需要提交
*.py[cod]  匹配 `*.pyc` / `*.pyo` / `*.pyd` 编译缓存文件
dist/      python 打包生成的发布目录（pip 打包出来的
08-14/back_up/ 项目里这个备份文件夹，不让 git 上传备份文件
build/ 打包构建临时文件夹，setup.py 编译产物

第3步：重命名分支并关联远程
git branch -M main   # 把当前分支（master）改名为 main，和 GitHub 默认一致
git remote add origin https://github.com/dreamsdrny/Bom-Check-PDF.git
第四步
git add .            # 把所有文件加入"暂存区"（准备好上传）
git commit -m "first commit"   # 打包成一个版本，附上说明
第五步：推送
git push -u origin main
push = 把本地提交上传到 GitHub
-u = 记住这是默认上传位置，以后直接 git push 就行
main = 推送到 main 分支
第六步：删除文件
git rm ".xlsx" "a.pdf"
git commit -m "remove sample BOM and PDF files"
git push

add：挑选要上传的文件
commit：在本地打包成一个版本
push：真正传到网上
最终的解释和完善对应的规则
git add .
git commit -m "说明"
git push

打包命令 
python -m PyInstaller --onefile --windowed --name Bom-Check-GUI --collect-all pikepdf bom_pdf_verify.py


python 关键字
1. `global`：修改**全局作用域（最外层）**变量
2. `nonlocal`：修改**外层嵌套函数（闭包外层）**的变量
3. 不加关键字：只读外层变量；想要赋值就会新建一个**局部变量**